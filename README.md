# DEMO

## Image Converter

This project provides a Qt-based desktop tool for converting images between many popular formats, including DDS and TGA. Drag an image into the window, choose the desired output format from the dropdown, and save the converted copy. The interface follows a ChatGPT-inspired grey theme with rounded corners.

### Running the app

```bash
pip install -r requirements.txt
python main.py
```